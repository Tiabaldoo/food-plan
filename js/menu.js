window.MenuEngine=(()=>{const days=['Понедельник','Вторник','Среда','Четверг','Пятница','Суббота','Воскресенье'];const slots=[['breakfast','Завтрак'],['snack','Перекус'],['lunch','Обед'],['dinner','Ужин']];function defaultState(){const selections={};days.forEach((d,di)=>{selections[di]={alcohol:false};slots.forEach(([type])=>selections[di][type]=di%RECIPES[type].length)});return{view:'today',selections}}function normalize(state){const base=defaultState();return{...base,...state,selections:{...base.selections,...(state.selections||{})}}}function replace(state,day,type){const list=RECIPES[type];const current=state.selections[day][type]||0;state.selections[day][type]=(current+1)%list.length;Store.save(state)}function toggleAlcohol(state,day){state.selections[day].alcohol=!state.selections[day].alcohol;Store.save(state)}function mealsFor(state,day){const d=state.selections[day];const meals=slots.map(([type,label])=>({type,label,recipe:RECIPES[type][d[type]||0]}));if(d.alcohol)meals.push({type:'alcohol',label:'Свободный вечер',recipe:RECIPES.alcohol[0]});return meals}function totals(state,day){return mealsFor(state,day).reduce((a,m)=>({ivan:a.ivan+m.recipe.kcal.ivan,wife:a.wife+m.recipe.kcal.wife}),{ivan:0,wife:0})}return{days,slots,defaultState,normalize,replace,toggleAlcohol,mealsFor,totals}})();