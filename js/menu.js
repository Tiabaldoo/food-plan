window.MenuEngine=(()=>{const days=['Понедельник','Вторник','Среда','Четверг','Пятница','Суббота','Воскресенье'];const slots=[['breakfast','Завтрак'],['snack','Перекус'],['lunch','Обед'],['dinner','Ужин']];function dayDefaults(di){const day={alcohol:false,eaten:{}};slots.forEach(([type])=>{day[type]=di%RECIPES[type].length;day.eaten[type]=false});return day}function defaultState(){const selections={};days.forEach((d,di)=>selections[di]=dayDefaults(di));return{view:'today',selections}}function normalize(state){const base=defaultState();const incoming=(state&&state.selections)||{};Object.keys(base.selections).forEach(day=>{const saved=incoming[day]||{};base.selections[day]={...base.selections[day],...saved,eaten:{...base.selections[day].eaten,...(saved.eaten||{})}}});return{...base,...(state||{}),selections:base.selections}}function replace(state,day,type){const list=RECIPES[type];const current=state.selections[day][type]||0;state.selections[day][type]=(current+1)%list.length;state.selections[day].eaten[type]=false;Store.save(state)}function toggleEaten(state,day,type){state.selections[day].eaten[type]=!state.selections[day].eaten[type];Store.save(state)}function isEaten(state,day,type){return Boolean(state.selections[day].eaten&&state.selections[day].eaten[type])}function toggleAlcohol(state,day){state.selections[day].alcohol=!state.selections[day].alcohol;Store.save(state)}function resetDay(state,day){state.selections[day]=dayDefaults(day);Store.save(state)}function mealsFor(state,day){const d=state.selections[day];const meals=slots.map(([type,label])=>({type,label,recipe:RECIPES[type][d[type]||0],eaten:isEaten(state,day,type)}));if(d.alcohol)meals.push({type:'alcohol',label:'Свободный вечер',recipe:RECIPES.alcohol[0],eaten:false});return meals}function totals(state,day){return mealsFor(state,day).reduce((a,m)=>({ivan:a.ivan+m.recipe.kcal.ivan,wife:a.wife+m.recipe.kcal.wife}),{ivan:0,wife:0})}return{days,slots,defaultState,normalize,replace,toggleEaten,isEaten,toggleAlcohol,resetDay,mealsFor,totals}})();